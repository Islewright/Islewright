name: macOS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CMAKE_TOOLCHAIN_FILE: ${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          # macOS 26.3 + Xcode 26.3
          - name: "macOS 26.3 + Xcode 26.3"
            os: macos-26
            compiler: xcode
            version: "26.3"
          # macOS 15.7.4 + Xcode 16.4
          - name: "macOS 15.7.4 + Xcode 16.4"
            os: macos-15
            compiler: xcode
            version: "16.4"
          # macOS 26.3 + gcc-15
          - name: "macOS 26.3 + gcc-15"
            os: macos-26
            compiler: gcc
            version: "15"
          # macOS 26.3 + gcc-14
          - name: "macOS 26.3 + gcc-14"
            os: macos-26
            compiler: gcc
            version: "14"
          # macOS 26.3 + gcc-13
          - name: "macOS 26.3 + gcc-13"
            os: macos-26
            compiler: gcc
            version: "13"

    runs-on: ${{ matrix.os }}
    name: 🍎 Build - ${{ matrix.name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11

      - name: Cache vcpkg binary packages
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/vcpkg-cache
          key: macos-vcpkg-${{ matrix.compiler }}-${{ matrix.version }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            macos-vcpkg-${{ matrix.compiler }}-${{ matrix.version }}-

      - name: Install system packages
        run: brew install autoconf automake autoconf-archive libtool ninja pkg-config

      - name: Configure Compiler
        run: |
          if [ "${{ matrix.compiler }}" = "gcc" ]; then
            brew install gcc@${{ matrix.version }}
            echo "CC=gcc-${{ matrix.version }}" >> "$GITHUB_ENV"
            echo "CXX=g++-${{ matrix.version }}" >> "$GITHUB_ENV"
          else
            sudo xcode-select -switch /Applications/Xcode_${{ matrix.version }}.app
            echo "CC=clang" >> "$GITHUB_ENV"
            echo "CXX=clang++" >> "$GITHUB_ENV"
          fi

      - name: Configure Build
        run: |
          export VCPKG_BINARY_SOURCES="clear;files,${{ github.workspace }}/vcpkg-cache,readwrite"
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE="$CMAKE_TOOLCHAIN_FILE"

      - name: Build
        run: cmake --build build --parallel
